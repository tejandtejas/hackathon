# Specification

> **Guidelines**: Read [guidelines.md](./guidelines.md) before executing ANY tasks below.

Check off items as completed.

## Solution Setup

- [x] Create asset directory: `mkdir -p assets/piva-agent/`
- [x] Invoke `setup-solution` skill to create `solution.yaml` and `asset.yaml` for `piva-agent`
- [x] Validate `assets/piva-agent/asset.yaml` and `solution.yaml` exist and are well-formed

## Asset Implementation

- [x] Execute `specification/piva-agent/specification.md` (all items)
